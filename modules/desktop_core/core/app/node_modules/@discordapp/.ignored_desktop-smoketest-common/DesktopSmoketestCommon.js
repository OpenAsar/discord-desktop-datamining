"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.DatedLogger = void 0;
class DatedLogger {
  constructor(name) {
    this.name = name;
  }
  getConsoleArgs(message, args) {
    return [`${DatedLogger.getDateTimeString()} ${this.name}: ${message}`, ...args];
  }
  log(message, ...args) {
    console.log.apply(console, this.getConsoleArgs(message, args));
  }
  info(message, ...args) {
    console.info.apply(console, this.getConsoleArgs(message, args));
  }
  warn(message, ...args) {
    console.warn.apply(console, this.getConsoleArgs(message, args));
  }
  error(message, ...args) {
    console.error.apply(console, this.getConsoleArgs(message, args));
  }
  static getDateTimeString() {
    const now = new Date();
    return now.toISOString();
  }
}
exports.DatedLogger = DatedLogger;